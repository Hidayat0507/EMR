// Tailwind v4 uses CSS-first configuration (tokens in @theme). No JS config needed.
// Keeping a minimal object to satisfy lint rules only.
const tailwindConfig = {} as const;
export default tailwindConfig;
